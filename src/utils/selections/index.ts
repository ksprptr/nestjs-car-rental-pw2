import * as user from './user.selection';
import * as brand from './brand.selection';
import * as color from './color.selection';
import * as address from './address.selection';
import * as country from './country.selection';
import * as vehicle from './vehicle.selection';

export const selections = {
  user,
  brand,
  color,
  address,
  country,
  vehicle,
};
